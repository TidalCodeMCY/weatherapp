export function createCurrent(
    currentTempF,
    currentTempC,
    currentFeelsF,
    currentFeelsC,
    currentHumidity,
    currentCondition
) {
    console.log(
        currentTempF,
        currentTempC,
        currentFeelsF,
        currentFeelsC,
        currentHumidity,
        currentCondition
    );
}

export function createCurrentLocation(
    currentCountry,
    currentTimeDate,
    currentName,
    currentRegion
) {
    console.log(currentCountry, currentTimeDate, currentName, currentRegion);
}
